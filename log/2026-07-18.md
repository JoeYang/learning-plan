# 2026-07-18 — C++ Session 14 (lock-free: SPSC, CAS, ABA)

**Learned:** memory-order selection is finally reflexive — own index relaxed, other's index acquire, publish with release — and rigtorp's index caches are the same MESI logic from false sharing, reused as an optimization: staleness is safe when the value is monotonic.

**Blocked:** the ABA *fix* didn't stick — chose "check after the CAS" twice in one day. The reflex to build: widen what the compare sees; an invariant must hold inside the atomic step.

**Next:** Phase 5 capstone (SPSC queue + latency histogram), then Session 15 reads a real protocol handler and closes the course.
