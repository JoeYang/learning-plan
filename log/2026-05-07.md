## 2026-05-07 — C++ Crash Course Session 10 (constexpr + Phase 3 close, capstone in flight)

- **Learned:** The compile-time safety net pattern for zero-copy parsing — `static_assert(sizeof) + static_assert(is_trivially_copyable_v)` together protect `reinterpret_cast<const Msg*>(buf)` from layout drift AND from someone adding a `std::string`/`std::unique_ptr`/virtual member that breaks memcpy semantics. Three concrete failure modes if the trivially-copyable assertion is missing: segfault on unmapped memory, silent garbage data on mapped memory, heap corruption when the destructor calls `free()` on a wire-bytes-as-pointer. Principle: *fail at compile time, not at market open*. Also: `constexpr` functions are dual-mode (compile-time when context demands, runtime otherwise) — `consteval` (C++20) is the keyword that forces compile-time-only. And `std::array` preserves size in the type across function boundaries while C arrays decay to `int*` and lose the size — same "decay" that bit on the SFINAE call exercise's `byteswap("hello")` trap from 2026-05-04.
- **Blocked:** Mechanism-precision under recall pressure — same residual as before but narrower. Q9 — knew `is_trivially_copyable` mattered, gave only "UB" without the segfault/silent/heap-corruption split. Q10 — CRTP nailed but tag-dispatch and `if constexpr` distinguishing constraints loose (described use case, not the *one thing each does that the other two cannot*). The win that did land: the absolute-overreach reading discipline that missed three times (Mock 1 Q5/Q19, S9 quiz Q3, S9 re-quiz Q3) finally caught the trap cleanly on Q3 today — "always" / "regardless of how it is called" / "no runtime fallback ever permitted" got tagged as false on first read.
- **Next:** Fill in the four TODO blocks in `template_playground.cpp` — CRTP handler, tag-dispatched router, static_assert-guarded POD, `if constexpr` byteswap. Compile clean at `-std=c++17 -Wall -Wextra`. Fill NOTES.md per-section. That closes Phase 3 and unlocks Session 11 (STL containers — vector / array / unordered_map / iterator invalidation).

S10 quiz: 9/10 (90%) — MCQ 7/7, short-answer 2/3 (Q9 ½ on failure-mode articulation, Q10 ½ on synthesis distinguishing constraints). Above the 7/10 gate by a comfortable margin.
