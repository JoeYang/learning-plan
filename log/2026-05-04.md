## 2026-05-04 — Claude Certified Architect Session 10 + 11 (Mock Exam 1 + Remediation Drills)

- **Learned:** The 5-layer enforcement model (prompt < schema < tool implementation < scope < `tool_choice`) is the right frame for every "where does it go" question. "Prompts are hope, not enforcement" stuck. Drill A confirmed I get the layer right ~88% of the time on first call — the actual gap is **stating the precise mechanism** that distinguishes adjacent layers (e.g., "scope is configuration-time access control; `tool_choice` operates over the already-scoped set and cannot subtract from that set"). Reading discipline for INCORRECT/NOT questions also clean: Drill B caught all four absolute-overreach traps without effort.
- **Blocked:** Tool-specific factual recall gaps surfaced in Drill B — `Grep`'s real `output_mode` is `files_with_matches`, not `filenames_only`; `detected_pattern` is qualitative evidence, *not* a numeric confidence score. Also conflated Pydantic's schema parts (types/enums) with its `@model_validator` (runtime → tool-implementation layer) on Drill A Q8.
- **Next:** Build the Pattern SDK project the remediation deck recommends. Plan now has Sessions 12–14 inserted: SDK Build A (programmatic prerequisites + PII boundaries), SDK Build B (internal retry + session isolation), SDK Build C (structured escalation + adversarial integration test). One pattern per session. Then Mock Exam 2 (Session 15) on Scenarios 2 and 5 under a real 60-min timer.

Mock Exam 1 score: 30 / 40 (75%) — below 80% bar. By scenario: S1 3/10, S3 9/10, S4 8/10, S6 10/10. Domain gaps: D5 Reliability 40%, D2 Tool Design 57%.

Drill A (layer-mapping, 8 reqs): 6 / 8 (75%). Pattern: correct layer most of the time; loses points on mechanism framing.
Drill B (subtly-wrong INCORRECT/NOT, 6 Q): 5 / 6 (83%). Reading discipline solid on absolute-overreach traps; misses on tool-specific facts (Q5 `detected_pattern` fields, Q6 Grep `output_mode`).

---

## 2026-05-04 — C++ Crash Course Session 9 (re-quiz, gate cleared)

- **Learned:** The header/source split is about the textual `#include` model, not just "interface vs implementation" — anything the compiler needs to see at every use site (templates, `inline` functions, `constexpr`, type definitions) goes in the header; anything that should be emitted once goes in the source file. The `inline` keyword is a *linker* directive ("multiple identical definitions allowed, dedupe at link time"), not a compiler hint about inlining the optimisation. The three-worlds frame (values / branches / types) finally cohered: `using Price = int64_t` lives in the name world and collapses; `struct Quantity { int64_t v; }` lives in the type world and stays distinct.
- **Blocked:** Mechanism-framing under quiz pressure is fragile. Picked "linker strips unused calls" for tag dispatch instead of the canonical "compile-time overload pick" (Q1) — the concept is right but the one-sentence mechanism didn't surface. Reverted to the SFINAE-runtime trap (Q3) despite a 30-minute refresher on exactly that — the absolute-overreach reading discipline that worked on Drill B atrophied across the gap. CRTP "what" is solid; "how" (`static_cast<Derived*>(this)`, indirect-call cost components) needs a one-sentence drill.
- **Next:** Session 10 — `constexpr` + `if constexpr`. Builds directly on Q7/Q10-strong territory; gate is cleared. Carry the two warnings into S10: (a) on negation questions, label every option's truthiness *first*, then pick the false one; (b) before any quiz, write the one-sentence mechanism for each pattern from memory.

S9 re-quiz score: 7.5 / 10 (above the 7/10 bar). MCQ 5/7, short-answer 2.5/3.

Refresher coverage that landed: header vs source file split, `inline`-as-linker-directive vs inlining-the-optimisation, `is_integral` precision (no floats, no pointers, no arrays), array-to-pointer decay in template arg deduction, alias-vs-strong-typedef, `if constexpr` dead-branch elision.
