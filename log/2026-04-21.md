## 2026-04-21 — Trading Algos Session 5 (Avellaneda-Stoikov)

- **Learned:** Market making is renting out balance sheet for a spread, not forecasting direction. The two-term optimal spread formula literally prices inventory risk (γσ²(T−t)) and adverse selection ((2/γ)ln(1+γ/κ)) as separate compensations — each term has a business reason.
- **Blocked:** The HJB derivation math itself (skipped the full step-by-step; derivation-writeup capstone deferred). Also fuzzy on when to use which γ-calibration method — match the calibration to the constraint being enforced.
- **Next:** Session 6 — inventory management and skewing in practice (GLFT extensions, what the textbook misses). Revisit the derivation math when in the mood for a workout.

Quiz score: 8.5 / 10 (above the 7/10 bar). Miss on Q7 (γ-calibration method match) and half-credit on Q9 (direct κ diagnostic vs markouts).

---

## 2026-04-21 — C++ Crash Course Session 9 (Tag Dispatch & Type Traits)

- **Learned:** Tag dispatch landed cleanly — overload resolution as a compile-time step on argument types, with the protocol-code corollary `static_assert(std::is_trivially_copyable_v<AddOrder>)` as a wire-format contract enforced by the compiler. `decltype` as an unevaluated type query, and `std::conditional_t` as a type-level ternary, both stuck.
- **Blocked:** SFINAE-as-compile-time and the three-worlds model (values / branches / types) didn't land. Quiz 6/10 with misses clustering: Q3 (fell for a "resolves … at runtime" distractor), Q6/Q10 (confused `using` alias with strong typedef despite the checkbox-6 discussion), Q8/Q9 (described behaviour without naming the SFINAE mechanism).
- **Next:** Deferred reading exercise — find a `<type_traits>` use in OSS protocol code, stub at `artefacts/cpp-crash-course/phase-3/type_traits_reading.md`. Before Session 10, revisit slides 6–7 of the phase-3 deck (SFINAE + enable_if) and the checkbox-5 "three worlds" recap; Session 10's `if constexpr` is introduced as "SFINAE's readable replacement" and leans directly on both.
- **Revisit:** Decision pending on Session 10 gating — option A (20-min review + Session 10 soon) vs option B (push Session 10 a day, use tomorrow for the reading exercise + review). Deferred; check this log entry before starting Session 10.

Quiz score: 6 / 10 (below the 7/10 bar). Session 9 checkboxes: 6/7 complete (reading exercise deferred).

