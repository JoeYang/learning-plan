# Adversarial Test Log — SDK Build A

> Captured after running `uv run pytest test_adversarial.py -v` against the
> completed implementation. Paste the raw output below; the README documents
> what the success criteria are.

## Run

```
$ uv run pytest test_adversarial.py -v
# TODO: paste output here
```

## Result summary

- All 10 jailbreak attempts: <PASS / FAIL>
- `session.refunds_without_prerequisite` final value: <N> (must be 0)
- Bypass counter test: <PASS / FAIL>

## Reflections

What surprised you. Which attempt was most interesting and why. Did you find
any pattern the tool *didn't* protect against (and is that intentional —
i.e., that pattern is the agent's job to handle, not the tool's)?
